import React from "react";

const Projects = ({ userInput }) => {
    return (
        <div className="pb-10">
            <h2 className="pb-2 text-gray-400">{"$ " + userInput}</h2>
            <h2 className="font-bold text-lg text-sky-200">To Be Updated</h2>
        </div>
    );
};

export default Projects;
